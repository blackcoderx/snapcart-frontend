

// a carousel component for the home page where items on promotion will be displayed

export default function Hero() {
    return (
        <section className="hero">
            hero
        </section>
    )
}